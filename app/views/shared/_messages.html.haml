- flash.each do |name, message|
  - if message.is_a?(String)
    - alert_class = name.to_s == 'notice' ? 'success' : 'danger'
    %div{:class => "alert alert-#{alert_class} alert-dismissible fade show border-0 rounded-0 mb-0", :role => "alert"}
      %strong
        = message
      %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"} ×
